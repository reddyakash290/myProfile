var hiddenBox = $(".events-ch");

$(".events-btn").click(function () {
  $(hiddenBox).toggle();
  hiddenBox.css("display", "grid");

  if (
    $(".events-btn").click(function () {
      $(hiddenBox).toggle();
    })
  );
});
